#ifndef __fmath__
#define __fmath__

#define IS_EVEN(n) n % 2 == 0
#define IS_ODD(n) n % 2 != 0

#define PINT(n) printf("%d\n", n);
#define PLONG(n) printf("%ld\n", n);

int is_prime(long n);

#endif
